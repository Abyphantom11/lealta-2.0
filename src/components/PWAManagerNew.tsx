/**
 * ❌ ARCHIVO DEPRECADO - NO USAR
 * 
 * Este archivo ha sido reemplazado por PWAProvider.tsx
 * Mantener solo para compatibilidad durante migración
 * 
 * USAR: @/providers/PWAProvider en su lugar
 */

'use client';

console.warn('⚠️ PWAManager.tsx está deprecado. Usar PWAProvider.tsx en su lugar');

// Componente de compatibilidad temporal - no hace nada
export default function PWAManagerDeprecated() {
  console.warn('⚠️ PWAManager está deprecado. Migrar a PWAProvider');
  return null;
}
